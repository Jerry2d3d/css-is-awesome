# v1.1 — Recipes Momentum

**Target release:** originally v1.1.0 — but that npm version number has been consumed: semantic-release published `css-is-awesome@1.1.0` on 2026-09-01 carrying the launch-wave work (theme rework, a11y fixes, linux baselines), not this backlog. This backlog ships under whatever later versions semantic-release derives from its commits.
**Theme:** Capitalize on v1.0 recipes momentum. Expand the recipe catalog, ship the install wizard, ~~scaffold the `@cia/a11y-recipes` add-on~~ (folded into the core book 2026-09-17), and prove the recipes → framework-pack codegen pattern with `@cia/react` v0.1.

> **Status: ✅ DONE — 9 of 9 epics resolved (7 complete, 1 retired, 1 deferred).** The last one closed 2026-09-25 when `create-cia@1.0.0` reached npm. v1.0 has shipped (v1.0.0 tagged 2026-08-17; launched 2026-09-01 with the first npm publish, public repo, and live docs site). Of the epics below, **EPIC-01 (batch 1) is complete** as of 2026-09-16 (all 7 recipes — `combobox-multiselect`, `breadcrumb`, `pagination`, `file-upload`, `toast`, `sortable-list`, `color-picker` — shipped with live demos, which took the book past the 28-recipe target; it stands at **32** today); **EPIC-08 is complete** (all 7 batch-2 recipes shipped 2026-09-10: `datepicker`, `data-table`, `confirm-dialog`, `admin-dashboard-layout`, `auth-flow`, `otp-input`, `multi-step-wizard` — 21 recipes now in the book total); **EPIC-09 (Playground)** is **Complete** as of 2026-09-17 (moved in from v1-0 EPIC-04 on 2026-09-10); **EPIC-07 (MCP zero-install)** is **Complete** as of 2026-09-11 (`css-is-awesome-mcp@1.0.1` published to npm; a same-named-bin collision with core was found and fixed the same day — see its epic file); EPIC-03 was **folded into the core book on 2026-09-17** (no separate package; `command-palette` shipped as a core recipe — the book is 32); EPIC-02 (install wizard) is **Complete** as of 2026-09-25 — `create-cia@1.0.0` published to npm with dist-tag `latest`, docs PR #15 merged, and CI now covering macOS alongside Ubuntu and Windows on Node 20 and 24 so the epic's three-platform requirement is met rather than assumed; **EPIC-04 (`@cia/react` codegen POC) is deferred permanently** as of 2026-09-19 — a time-boxed spike failed the epic's own fail-fast clause, see its decision block — and **EPIC-06 (the blog's two tracks) is Complete** as of 2026-09-19 (the index splits by track, the three seed discovery posts are live, `feed.xml` is served, and the cadence + publish rules are recorded in the blog README — the work had shipped incrementally across September and only the epic's checkboxes were stale; the 2026-09-19 pass verified all five stories and closed the one real gap, the reverse cross-links from the browser-support matrix). **The density knob (EPIC-05)** — queued into this version 2026-08-29 (f9822be) — is **Complete** as of 2026-09-10 (all 24 themes derive `--space-1..9` from one `--space-unit` via `calc()`, a density slider lives in the theme editor, and `/docs/authoring/themes` documents the three levels of override — all verified live; a real pre-existing bug where no theme-editor row actually applied its edit to the live preview was found and fixed along the way). The remaining v1-0 carried-forward stories (see [v1-0/README.md](../v1-0/README.md)) compete with this backlog for priority once real user signal arrives.

## Epics

| # | Epic | Mission | Effort | Stories |
|---|---|---|---|---|
| [01](./EPIC-01-additional-recipes.md) | **Additional Recipes (batch 1)** | 7 more recipes: combobox-multiselect, breadcrumb, pagination, file-upload, toast, sortable list, color-picker. **Complete 2026-09-16** — all 7 shipped with live demos, validator 31/31 green. | ~10-14 days | 14 |
| [02](./EPIC-02-install-wizard.md) | **`npm create cia` wizard** | Guided install: framework? theme? MCP? Wires SCSS entry + theme attr in one command. **✅ Complete — `create-cia@1.0.0` on npm 2026-09-25.** Zero runtime deps; CI green on Ubuntu, Windows **and macOS** × Node 20 + 24. Docs PR #15 merged. The a11y-add-on prompt was retired with EPIC-03 and is recorded as a deviation, not as outstanding work. | ~3-5 days | 7 |
| [03](./EPIC-03-cia-a11y-recipes.md) | **`@cia/a11y-recipes` add-on** | ⛔ **Folded into the core book 2026-09-17** — no separate package. WCAG-strict content lives inside each recipe as a Variants subsection; `command-palette` (with its native `<dialog>` focus trap) shipped as a core recipe the same day. See the decision block in the epic file. | — | 10 (retired) |
| [04](./EPIC-04-framework-pack-react.md) | **`@cia/react` v0.1 (POC)** | ⛔ **Deferred permanently 2026-09-19** on the epic's own fail-fast clause. A time-boxed spike ([`spike/`](../../../spike/README.md)) built F4.1 as specified and ran it over three recipes: 2 of 3 generated components crash on render, the third ships a hard-coded pizza-toppings picker, and the "codegen" turned out to be the recipe's React block copied verbatim under a 15-line template identical across every output. `@boilerai/react` 0.2.1 already publishes 103 components against cia. See the decision block. | — | 12 (retired) |
| [06](./EPIC-06-blog.md) | **The blog: two tracks** | Engineering-the-system posts + NEW CSS-discoveries track; index split, feed.xml, 3 seed posts. **Complete 2026-09-19** — shipped as three tracks (engineering / CSS / AI), 3 discovery posts live, Atom feed at `/feed.xml`. | S | 5 |
| [05](./EPIC-05-density-knob.md) | **The density knob (`--space-unit`)** | One variable rescales the whole spacing system. `--space-0..9` become `calc()` over a master unit; set it once and the UI tightens or opens up. Density changes a page's character more than colour does. **Complete 2026-09-10** — all 24 themes converted, editor slider + docs shipped, both verified live; also fixed a pre-existing bug where no theme-editor row actually applied live. | ~1-2 days | 4 |
| [07](./EPIC-07-mcp-zero-install.md) | **MCP that just works** | `npx css-is-awesome-mcp` with no manual `sdk`+`zod` install — a dedicated add-on package, without adding JS deps to the core package. From the 2026-09-07 external review; pull-forward DX. **✅ Complete 2026-09-11** — `css-is-awesome-mcp@1.0.1` published to npm, verified with a real fresh `npx` call; a same-named `bin` collision with core (silently ran the wrong file) was found and fixed same-day, with a regression test to match (see the epic file + the blog post). | ~1-2 days | 4 |
| [08](./EPIC-08-additional-recipes-batch-2.md) | **Additional Recipes (batch 2, from Boiler)** | 7 more recipes, each backed by a real reference implementation surveyed from `boiler-project-ai` via its own MCP server: datepicker, data-table (both close the original, never-built v1.0-queued gap), admin-dashboard-layout, confirm-dialog, auth-flow, multi-step-wizard, otp-input. **Complete 2026-09-10** — all 7 shipped, all verified live. | ~9-13 days | 14 |
| [09](./EPIC-09-playground.md) | **Playground** | `/playground` page — paste SCSS using cia mixins, live-compiled preview, share via URL. Moved here 2026-09-10 from v1-0 EPIC-04. **Complete 2026-09-17** — CodeMirror 6 + dart-sass in a worker + sandboxed preview + share links; every recipe page has a "Try in playground" button. | ~4-6 days | 7 |

**Total v1.1 effort:** ~40-58 working days. **Total stories:** 72.

## Definition of done for v1.1

- [x] 28 recipes total in the recipe book — **31 shipped as of 2026-09-16** (batch 1 + batch 2 both Complete; the extra come from the v1.2 form-validation and i18n recipes). This line previously said "12 total (5 from v1.0 + 7 new)," which was stale: only `dialog` and `combobox` ever shipped in v1.0; everything else shipped across later, unplanned-against-this-backlog work — see `roadmap/epics/README.md`'s audit notes for the real history)
- [ ] `npm create cia@latest` ships and works on Mac/Linux/Windows
- [x] ~~`@cia/a11y-recipes` npm package published~~ — retired 2026-09-17 (folded into the core book; see EPIC-03)
- [ ] `@cia/react` v0.1 published with 3-5 generated components
- [ ] CHANGELOG.md entry for this backlog's release (note: CHANGELOG already has a `1.1.0` entry dated 2026-09-01 — that's the launch wave, not this backlog)
- [ ] No regression in v1.0 epic DoDs
